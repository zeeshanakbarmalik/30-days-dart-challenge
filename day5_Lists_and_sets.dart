void main() {
  /*
   * Use .add() method to store a new value to the list index.
   * Use .remove() method to remove any value from the list index.
   * Use .removeLast() method to remove the same numbers in 
   * different index/positions.
   * <variable>.shuffle()
   * Use .length in print statement with the variable name.
   * Use .indexOf(5) in print statement with the variable name.
   * To update or reassign a value from an index use variable name 
   * with index number. (e.g. score[1] = value;)  
   */
}
